name: CI

on:
  push:
    branches:
      - main
    paths:
      - "mobility-reliability-platform/**"
      - ".github/workflows/ci.yml"
  pull_request:
    branches:
      - main
    paths:
      - "mobility-reliability-platform/**"
      - ".github/workflows/ci.yml"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: mobility-reliability-platform

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Set up Java for Firebase Emulator
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: 1.26.5
          cache: false

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run checks
        run: pnpm check

      - name: Build platform bundles
        run: pnpm build

      - name: Run tests
        run: pnpm test

      - name: Check telemetry gateway
        working-directory: mobility-reliability-platform/services/telemetry-gateway
        run: |
          test -z "$(gofmt -l ./cmd ./internal)"
          go vet ./...
          go test -race ./...
          CGO_ENABLED=0 go build -trimpath -o /tmp/telemetry-gateway ./cmd/server
