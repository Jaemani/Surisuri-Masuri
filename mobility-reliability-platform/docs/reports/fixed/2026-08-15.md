# 2026년 8월 1차 기술 정기리포트

> 보고서 ID: R07
> 계획 기준일: 2026-08-15
> 실제 세션·발행일: 미입력 — 실제 수행 여부 사람 확인 필요
> 검토 상태: draft
> 문서 상태: 계획 기준 사전작성본
> 내용 상태: planned
> 검토자: 미입력
> 검토 일시: 미입력
> 검토 결과: 미검토
> 실제 증빙: 회의 후 갱신

## 목적

GPS·motion 특성으로 수집 데이터의 품질과 이동 유형을 보조 판별하는 학습 문제를 정의한다. 모델보다 먼저 라벨, split, baseline, 실패 유형을 고정해 데이터 누수와 과장된 성능을 방지한다.

## 계획상 논의

- 전동보장구 주행, 도보, 차량, 정지, 불확실 구간의 라벨 정의
- 위치 정확도, 속도 분포, 가속·감속, 정지 비율, 방향 변화, 이동거리 대비 변위 특성
- 동일 사용자·동일 경로가 train/test에 섞이지 않도록 group/time split 적용
- 규칙 기반 baseline과 로지스틱 회귀, 작은 1D CNN/GRU 후보 비교
- 불확실 구간을 억지 분류하지 않는 abstain 라벨
- 사용자 동의와 라벨링 데이터의 보존·삭제 원칙
- BigQuery 활성화 전에는 승인된 Cloud Storage manifest를, 활성화 후에는 비용이 기록된 partitioned snapshot만 사용하는 학습 데이터 경계

## 결정 예정 항목

- 모델이 답해야 할 질문과 답하지 않을 질문
- 라벨링 가이드, 검토 절차, disagreement 처리
- 최소 데이터량 이전에 사용할 규칙 기반 fallback
- primary metric과 class imbalance 대응
- 재현 가능한 학습 환경, Storage manifest 또는 optional BigQuery snapshot, 데이터셋 버전 규칙

## 기대 산출물

- 원본 Storage object 계보와 manifest ID를 포함한 데이터셋 카드 초안 및 라벨링 도구 또는 화면
- 규칙 기반 품질 판별 baseline
- PyTorch 최소 후보 학습 결과와 baseline 대비 동일 split 비교표
- 후보 모델의 선택 또는 배포 유보 판단과 근거
- 데이터 split 및 leakage 검사
- 대표 실패 유형 목록과 confusion matrix 템플릿

## 검증 지표

- 두 라벨러 또는 재검토 간 일치도를 기록할 수 있는가
- 사용자·시간 기준 split이 데이터 누수를 차단하는가
- class별 precision/recall과 abstain 비율이 함께 보고되는가
- baseline 오류가 원시 구간과 연결되어 재현 가능한가
- 최소 PyTorch 후보가 baseline과 같은 평가셋·지표로 비교되고 선택 또는 유보 결정이 남았는가

## 실제 진행 입력란

### 2026-08-13 코드 snapshot 추가

- 계획의 ML 의제와 별개로, 제품 핵심을 복지관 중심 수리 운영으로 바로잡은 뒤 선행해야 할 서버 command 경계를 구현했다.
- 수리 접수·상태 전환·지원금 거래를 Firebase Auth·App Check·membership·assignment·idempotency 검증 뒤 Firestore transaction으로 기록한다.
- 모바일·콘솔에는 ID token/App Check/HTTP endpoint를 주입하는 운영 repository adapter를 추가했으며, production 오류 시 합성 demo로 자동 후퇴하지 않는다.
- 이는 계획 주제의 변경이나 ML 완료 주장이 아니다. R07의 품질 모델·ONNX 계획은 아래 기존 상태처럼 별도 유지된다.
- 근거: [EVD-20260813-02/03](../../evidence/2026-08-product.md), source commits `f83612f`, `e1df576`, 시각 회귀 `0378d1e`.

- 실제 수행 내용: R07-A 데이터 계약 기반을 source commit `a20a85b`에서 구현했다.
  `quality-label.v1`, `quality-dataset-manifest.v1`, deterministic generator, dataset↔manifest
  validator와 locked Python test 환경까지 완료했다. 라벨링 UI, feature extractor, rules
  baseline과 PyTorch 후보 비교는 아직 미착수다.
- 데이터/라벨 수와 Storage manifest 또는 BigQuery snapshot 출처: seed `20260811`로 만든
  synthetic trace 48개·sample 576개다. 네 known class가 각각 12개다. 실제 사용자,
  developer-device, Storage, BigQuery, legacy 수리데이터를 사용하지 않았다.
- split 및 baseline 결과: `group-time-holdout.v1`으로 train/validation/test 각 16개,
  group leakage 0을 test로 확인했다. 규칙·모델 baseline 수치와 confusion matrix는 아직
  없으므로 성능을 주장하지 않는다.
- 후보 모델 비교와 선택/유보 결정: 미실행. R07-B feature/rules baseline과 R07-C PyTorch
  비교 전까지 모델 선택과 ONNX 진입을 유보한다.
- 실제 결정 및 보류: `unknown_review_required`를 학습 class가 아닌 review/abstain 상태로
  두고, `developer_device`는 benchmark loader에서 거부한다. 합성 생성물은 Git에 넣지
  않고 code/schema/lock/test만 version 관리한다.
- 데이터 편향·누수 위험: 가상 `(0, 0)` 주변의 의도적으로 분리된 synthetic pattern이라
  실제 주행 분포와 일반화 성능을 나타내지 않는다. 모든 sample 시간, scenario group,
  source와 manifest hash를 검사하지만 field bias는 실제 동의·manifest가 생긴 뒤 별도
  평가해야 한다.

## 실제 주장별 증빙 입력란

| Evidence ID | 상태 | 환경 | 링크/메모 |
| --- | --- | --- | --- |
| [EVD-20260811-002](../../evidence/2026-08.md#evd-20260811-002--r07-합성-품질-데이터셋-계약과-결정론적-split) | generated / 사람 검토 대기 | WSL2 / Python 3.12.3 / synthetic only | commit `a20a85b`, 26 Python tests, 11 contract fixtures, embedded dataset hash 기록 |

이 입력란은 2026-08-11의 코드·테스트 snapshot이다. 실제 8월 15일 세션, 검토자,
회의 여부를 의미하지 않으며 상단의 발행·검토 필드는 실제 수행 후 사람이 입력한다.

## 다음 회차

R07에서 선택된 PyTorch 모델이 있을 때만 ONNX로 변환해 React Native 앱에서 실행한다. 선택이 유보되면 rules fallback을 기준선으로 유지한 채 export 가능성과 모델 크기, 지연시간, 메모리, 양자화 전후 성능을 비교할 예정이다.
