import { useRef, useState } from 'react';
import * as Crypto from 'expo-crypto';
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';

import type { RepairerJobCommand, RepairJob } from '../types';
import { normalizeDeviceCode } from '../deviceCode';
import { createDefaultSchedule, isScheduleInRange, scheduleBounds } from '../schedule';
import { draftsFromRepairItems, emptyWorkItemDraft, parseWorkItemDrafts, type WorkItemDraft } from '../workItemDraft';
import { DeviceCodeScanner } from './DeviceCodeScanner';
import { Card, colors, DemoBadge, ProductButton, SectionHeading, StatusPill } from './ProductUi';
import { VisitDateTimePicker } from './VisitDateTimePicker';

type Props = { jobs: RepairJob[]; displayName: string; isDemo: boolean; onSwitchToUser: () => void; onTransition: (command: RepairerJobCommand) => Promise<unknown>; onRefresh: () => Promise<unknown> };
type Panel = 'detail' | 'schedule' | 'submit' | 'submitReview';

export function RepairerScreen({ jobs, displayName, isDemo, onSwitchToUser, onTransition, onRefresh }: Props) {
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const [panel, setPanel] = useState<Panel>('detail');
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const selected = jobs.find((job) => job.id === selectedId) ?? null;
  const select = (id: string) => { setSelectedId(id); setPanel('detail'); setError(null); };
  const run = async (command: RepairerJobCommand) => {
    setBusy(true); setError(null);
    try { await onTransition(command); setPanel('detail'); }
    catch (reason) {
      const code = reason && typeof reason === 'object' && 'code' in reason ? String((reason as { code: unknown }).code) : '';
      setError(code === 'REVISION_CONFLICT' ? '다른 담당자가 먼저 변경했어요. 최신 상태를 불러와 주세요.' : code === 'PROJECTION_PENDING' ? '처리는 전달됐을 수 있어요. 새로 보내지 말고 상태를 확인해 주세요.' : '입력 내용은 유지했어요. 연결을 확인한 뒤 다시 시도해 주세요.');
    } finally { setBusy(false); }
  };

  if (selected) return <RepairerJobWorkspace job={selected} panel={panel} busy={busy} error={error} onBack={() => setSelectedId(null)} onPanel={setPanel} onRun={run} onRefresh={onRefresh} />;

  return <View style={styles.screen}><ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}>
    <View style={styles.topBar}><View><Text style={styles.kicker}>{displayName}</Text><Text accessibilityRole="header" style={styles.title}>오늘의 작업</Text><Text style={styles.subtitle}>배정된 수리를 한 단계씩 처리하세요.</Text></View>{isDemo ? <Pressable accessibilityRole="button" accessibilityLabel="사용자 화면으로 전환" onPress={onSwitchToUser} style={styles.roleButton}><Text style={styles.roleButtonText}>사용자 화면</Text></Pressable> : null}</View>
    {isDemo ? <DemoBadge label="수리사 역할 · 합성 데모" /> : null}
    <Card style={styles.identityCard}><View style={styles.identityIcon}><Text style={styles.identityGlyph}>✓</Text></View><View style={styles.identityCopy}><Text style={styles.identityTitle}>배정된 작업만 표시돼요</Text><Text style={styles.identityText}>고객 연락처와 지원금 잔액 없이 공개코드로 기기를 대조합니다.</Text></View></Card>
    <SectionHeading title="처리할 작업" action={`${jobs.length}건`} onAction={() => undefined} />
    <View style={styles.jobList}>{jobs.map((job) => <RepairJobCard key={job.id} job={job} onOpen={() => select(job.id)} />)}</View>
    {jobs.length === 0 ? <Card><Text style={styles.emptyTitle}>현재 배정된 작업이 없어요</Text><Text style={styles.emptyText}>복지관에서 새 작업을 배정하면 이곳에 표시됩니다.</Text></Card> : null}
    {isDemo ? <DemoBadge label="표시된 고객·작업 정보는 합성 데이터입니다" /> : null}
  </ScrollView></View>;
}

function RepairJobCard({ job, onOpen }: { job: RepairJob; onOpen: () => void }) {
  const status = statusCopy(job.status);
  return <Card style={styles.jobCard}><View style={styles.jobTop}><StatusPill label={status.label} tone={status.tone} /><Text style={styles.jobDue}>{job.scheduleLabel}</Text></View><Text style={styles.customer}>{job.customerLabel}</Text><Text style={styles.device}>{job.device.model} · {job.device.publicCode}</Text><View style={styles.issueRow}><View style={styles.issueDot} /><Text style={styles.issue}>{job.issue}</Text></View><ProductButton label={primaryLabel(job)} onPress={onOpen} variant="secondary" /></Card>;
}

function RepairerJobWorkspace({ job, panel, busy, error, onBack, onPanel, onRun, onRefresh }: { job: RepairJob; panel: Panel; busy: boolean; error: string | null; onBack: () => void; onPanel: (panel: Panel) => void; onRun: (command: RepairerJobCommand) => Promise<void>; onRefresh: () => Promise<unknown> }) {
  const [scheduleAt, setScheduleAt] = useState(() => createDefaultSchedule());
  const [workItemDrafts, setWorkItemDrafts] = useState(() => draftsFromRepairItems(job.workItems));
  const keyRef = useRef<{ signature: string; value: string } | null>(null);
  const [deviceCode, setDeviceCode] = useState('');
  const [scannerOpen, setScannerOpen] = useState(false);
  const deviceVerified = normalizeDeviceCode(deviceCode) === normalizeDeviceCode(job.device.publicCode);
  const commandKey = (signature: string) => {
    if (keyRef.current?.signature === signature) return keyRef.current.value;
    const value = Crypto.randomUUID();
    keyRef.current = { signature, value };
    return value;
  };
  const parsedWork = parseWorkItemDrafts(workItemDrafts);
  const status = statusCopy(job.status);
  const action = job.allowedActions[0];
  const bounds = scheduleBounds();
  const scheduleValid = isScheduleInRange(scheduleAt);
  return <View style={styles.screen}><ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}>
    <Pressable accessibilityRole="button" onPress={onBack} style={styles.backButton}><Text style={styles.backText}>‹ 작업 목록</Text></Pressable>
    <View style={styles.detailHeader}><StatusPill label={status.label} tone={status.tone} /><Text accessibilityRole="header" style={styles.detailTitle}>{job.issue}</Text><Text style={styles.detailMeta}>{job.customerLabel} · {job.device.publicCode}</Text></View>
    <Card style={styles.verifyCard}><Text style={styles.cardKicker}>현장 기기 확인</Text><Text style={styles.verifyCode}>{job.device.publicCode}</Text><Text style={styles.verifyModel}>{job.device.model}</Text><Text style={styles.verifyHelp}>기기에 붙은 공개코드를 입력하거나 QR을 스캔하세요.</Text><TextInput accessibilityLabel="현장 기기 공개코드" autoCapitalize="characters" autoCorrect={false} value={deviceCode} onChangeText={(value) => setDeviceCode(value)} placeholder="예: MR-2208" placeholderTextColor="#8EA3A4" style={styles.darkInput}/><Pressable accessibilityRole="button" onPress={() => setScannerOpen(true)} style={styles.scanButton}><Text style={styles.scanButtonText}>QR 카메라로 확인</Text></Pressable><View accessibilityLiveRegion="polite" style={[styles.verifyStatus, deviceVerified ? styles.verifyStatusOk : styles.verifyStatusIdle]}><Text style={[styles.verifyStatusText, deviceVerified && styles.verifyStatusTextOk]}>{deviceVerified ? '✓ 기기 코드가 일치합니다' : deviceCode ? '코드가 일치하지 않습니다' : '코드를 확인해야 다음 단계로 갈 수 있어요'}</Text></View></Card>
    {scannerOpen ? <DeviceCodeScanner onCode={setDeviceCode} onClose={() => setScannerOpen(false)} /> : null}
    <View style={styles.stageRow}>{['배정', '일정', '작업', '검증'].map((label, index) => <View key={label} style={styles.stageItem}><View style={[styles.stageDot, index <= stageIndex(job.status) && styles.stageDotActive]}><Text style={[styles.stageNumber, index <= stageIndex(job.status) && styles.stageNumberActive]}>{index + 1}</Text></View><Text style={styles.stageLabel}>{label}</Text></View>)}</View>
    <Card style={styles.actionCard}><Text style={styles.cardKicker}>현재 단계</Text><Text style={styles.actionTitle}>{status.title}</Text><Text style={styles.actionText}>{status.description}</Text><View style={styles.factRow}><Text style={styles.factLabel}>방문 일정</Text><Text style={styles.factValue}>{job.scheduleLabel}</Text></View>{job.billedAmountKrw !== null ? <View style={styles.factRow}><Text style={styles.factLabel}>제출 금액</Text><Text style={styles.factValue}>{job.billedAmountKrw.toLocaleString('ko-KR')}원</Text></View> : null}</Card>
    {error ? <View accessibilityRole="alert" style={styles.errorBox}><Text style={styles.errorText}>{error}</Text><Pressable accessibilityRole="button" onPress={() => void onRefresh()}><Text style={styles.refreshText}>최신 상태 불러오기</Text></Pressable></View> : null}
    {action === 'schedule' && panel === 'detail' ? <ProductButton label={deviceVerified ? '방문 일정 확정' : '기기 코드 확인 필요'} disabled={!deviceVerified} onPress={() => onPanel('schedule')} /> : null}
    {action === 'schedule' && panel === 'schedule' ? <Card style={styles.formCard}><Text accessibilityRole="header" style={styles.formTitle}>방문 일정 선택</Text><Text style={styles.safeCopy}>현재 시각 이후부터 180일 안에서 방문 날짜와 시간을 정하세요.</Text><VisitDateTimePicker value={scheduleAt} minimumDate={bounds.minimum} maximumDate={bounds.maximum} onChange={setScheduleAt} />{!scheduleValid ? <Text accessibilityRole="alert" style={styles.scheduleError}>현재 시각 이후부터 180일 안의 일정을 선택해 주세요.</Text> : null}<ProductButton label={busy ? '확정하는 중…' : '이 일정으로 확정'} disabled={busy || !scheduleValid} onPress={() => { const scheduledAt = scheduleAt.toISOString(); void onRun({ action: 'schedule', repairRequestId: job.id, expectedRevision: job.revision, scheduledAt, idempotencyKey: commandKey(`schedule:${job.revision}:${scheduledAt}`) }); }} /></Card> : null}
    {action === 'start' ? <ProductButton label={busy ? '시작하는 중…' : deviceVerified ? '현장 확인 후 작업 시작' : '기기 코드 확인 필요'} disabled={busy || !deviceVerified} onPress={() => void onRun({ action: 'start', repairRequestId: job.id, expectedRevision: job.revision, idempotencyKey: commandKey(`start:${job.revision}`) })} /> : null}
    {action === 'resume' ? <ProductButton label={busy ? '불러오는 중…' : '수정 작업 다시 시작'} disabled={busy} onPress={() => void onRun({ action: 'resume', repairRequestId: job.id, expectedRevision: job.revision, idempotencyKey: commandKey(`resume:${job.revision}`) })} /> : null}
    {action === 'submit' && panel === 'detail' ? <ProductButton label="비용 입력 및 제출" onPress={() => onPanel('submit')} /> : null}
    {action === 'submit' && panel === 'submit' ? <Card style={styles.formCard}><Text accessibilityRole="header" style={styles.formTitle}>수리 작업 항목</Text><Text style={styles.safeCopy}>부위·처리 방법·수량·금액을 항목별로 남깁니다. 총 청구액은 항목 합계로 계산됩니다.</Text><View style={styles.itemList}>{workItemDrafts.map((draft, index) => <WorkItemEditor key={draft.id} draft={draft} index={index} removable={workItemDrafts.length > 1} onChange={(next) => setWorkItemDrafts((current) => current.map((item) => item.id === next.id ? next : item))} onRemove={() => setWorkItemDrafts((current) => current.filter((item) => item.id !== draft.id))} />)}</View><Pressable accessibilityRole="button" accessibilityState={{ disabled: workItemDrafts.length >= 20 }} disabled={workItemDrafts.length >= 20} onPress={() => setWorkItemDrafts((current) => [...current, emptyWorkItemDraft(Crypto.randomUUID())])} style={[styles.addItemButton, workItemDrafts.length >= 20 && styles.disabledButton]}><Text style={styles.addItemText}>+ 작업 항목 추가</Text></Pressable><View style={styles.totalBox}><Text style={styles.totalLabel}>총 청구액</Text><Text accessibilityLiveRegion="polite" style={styles.totalValue}>{parsedWork.total.toLocaleString('ko-KR')}원</Text></View>{!parsedWork.valid ? <Text accessibilityRole="alert" style={styles.formError}>각 항목의 수량과 금액을 확인하고 총액을 0원보다 크게 입력해 주세요.</Text> : null}<ProductButton label="입력 내용 검토" disabled={busy || !parsedWork.valid} onPress={() => onPanel('submitReview')} /></Card> : null}
    {action === 'submit' && panel === 'submitReview' ? <Card style={styles.formCard}><Text accessibilityRole="header" style={styles.formTitle}>제출 전 확인</Text><Text style={styles.safeCopy}>복지관이 검증할 작업 내역입니다. 제출 후에는 복지관 확인 전까지 읽기 전용으로 표시됩니다.</Text><View style={styles.reviewList}>{parsedWork.items.map((item, index) => <View key={`${item.categoryCode}-${item.actionCode}-${index}`} style={styles.reviewItem}><Text style={styles.reviewItemTitle}>{index + 1}. {item.categoryLabel} · {item.actionLabel}</Text><Text style={styles.reviewItemMeta}>수량 {item.quantity} · {item.lineAmountKrw.toLocaleString('ko-KR')}원</Text></View>)}</View><View style={styles.totalBox}><Text style={styles.totalLabel}>총 청구액</Text><Text style={styles.totalValue}>{parsedWork.total.toLocaleString('ko-KR')}원</Text></View><Pressable accessibilityRole="button" disabled={busy} onPress={() => onPanel('submit')} style={styles.editButton}><Text style={styles.editButtonText}>수정하기</Text></Pressable><ProductButton label={busy ? '제출하는 중…' : '이 내용으로 제출'} disabled={busy || !parsedWork.valid} onPress={() => { const signature = `submit:${job.revision}:${JSON.stringify(parsedWork.items)}:${parsedWork.total}`; void onRun({ action: 'submit', repairRequestId: job.id, expectedRevision: job.revision, billedAmountKrw: parsedWork.total, workItems: parsedWork.items, idempotencyKey: commandKey(signature) }); }} /></Card> : null}
    {!action ? <Card style={styles.waitCard}><Text style={styles.waitTitle}>{job.status === 'center_verified' ? '복지관 확인이 끝났어요' : '복지관 검증을 기다리고 있어요'}</Text><Text style={styles.safeCopy}>제출한 기록은 읽기 전용입니다. 지원금 검증과 최종 완료는 복지관에서 처리합니다.</Text></Card> : null}
    {busy ? <ActivityIndicator color={colors.teal} style={styles.loader} /> : null}
  </ScrollView></View>;
}

function statusCopy(status: RepairJob['status']): { label: string; title: string; description: string; tone: 'teal' | 'orange' | 'blue' | 'neutral' } {
  if (status === 'assigned') return { label: '일정 필요', title: '방문 일정을 확정해 주세요', description: '복지관이 배정한 작업입니다. 기기 공개코드를 확인하고 일정을 정하세요.', tone: 'orange' };
  if (status === 'scheduled') return { label: '방문 예정', title: '현장 확인 후 작업을 시작하세요', description: '기기 공개코드가 일치할 때만 작업을 시작합니다.', tone: 'blue' };
  if (status === 'in_progress') return { label: '작업 중', title: '작업을 마치면 결과를 제출하세요', description: '작업 부위·처리 방법·금액을 남기면 복지관 검증이 이어집니다.', tone: 'teal' };
  if (status === 'needs_correction') return { label: '수정 필요', title: '복지관 요청을 확인해 주세요', description: '수정 사유의 구조화된 전달은 다음 계약에서 추가합니다.', tone: 'orange' };
  if (status === 'center_verified') return { label: '기관 확인', title: '복지관 확인이 끝났어요', description: '최종 완료 기록을 기다리는 단계입니다.', tone: 'teal' };
  return { label: '검증 대기', title: '비용을 제출했어요', description: '복지관이 금액과 지원 여부를 확인하고 있습니다.', tone: 'neutral' };
}
function primaryLabel(job: RepairJob) { const action = job.allowedActions[0]; return action === 'schedule' ? '일정 정하기' : action === 'start' ? '작업 열기' : action === 'submit' ? '작업 계속하기' : action === 'resume' ? '수정 요청 확인' : '제출 내용 보기'; }
function stageIndex(status: RepairJob['status']) { return status === 'assigned' ? 0 : status === 'scheduled' ? 1 : status === 'in_progress' || status === 'needs_correction' ? 2 : 3; }
const workCategories = [{ code: 'wheel_tire', label: '바퀴·타이어' }, { code: 'battery', label: '배터리' }, { code: 'brakes', label: '브레이크' }, { code: 'controls', label: '조작부' }, { code: 'seat_frame', label: '시트·프레임' }, { code: 'other', label: '기타' }] as const;
const workActions = [{ code: 'inspect', label: '점검' }, { code: 'adjust', label: '조정' }, { code: 'repair', label: '수리' }, { code: 'replace', label: '교체' }] as const;

function WorkItemEditor({ draft, index, removable, onChange, onRemove }: { draft: WorkItemDraft; index: number; removable: boolean; onChange: (draft: WorkItemDraft) => void; onRemove: () => void }) {
  const itemNumber = index + 1;
  const update = (patch: Partial<WorkItemDraft>) => onChange({ ...draft, ...patch });
  return <View style={styles.itemCard}>
    <View style={styles.itemHeader}><Text accessibilityRole="header" style={styles.itemTitle}>작업 항목 {itemNumber}</Text>{removable ? <Pressable accessibilityRole="button" accessibilityLabel={`작업 항목 ${itemNumber} 삭제`} onPress={onRemove} style={styles.removeButton}><Text style={styles.removeText}>삭제</Text></Pressable> : null}</View>
    <Text style={styles.inputLabel}>작업 부위</Text><View accessibilityRole="radiogroup" accessibilityLabel={`작업 항목 ${itemNumber} 작업 부위`} style={styles.chips}>{workCategories.map((item) => <Pressable key={item.code} accessibilityRole="radio" accessibilityLabel={`작업 항목 ${itemNumber} ${item.label}`} accessibilityState={{ checked: draft.categoryCode === item.code }} onPress={() => update({ categoryCode: item.code })} style={[styles.chip, draft.categoryCode === item.code && styles.chipActive]}><Text style={[styles.chipText, draft.categoryCode === item.code && styles.chipTextActive]}>{item.label}</Text></Pressable>)}</View>
    <Text style={styles.inputLabel}>처리 방법</Text><View accessibilityRole="radiogroup" accessibilityLabel={`작업 항목 ${itemNumber} 처리 방법`} style={styles.chips}>{workActions.map((item) => <Pressable key={item.code} accessibilityRole="radio" accessibilityLabel={`작업 항목 ${itemNumber} ${item.label}`} accessibilityState={{ checked: draft.actionCode === item.code }} onPress={() => update({ actionCode: item.code })} style={[styles.chip, draft.actionCode === item.code && styles.chipActive]}><Text style={[styles.chipText, draft.actionCode === item.code && styles.chipTextActive]}>{item.label}</Text></Pressable>)}</View>
    <View style={styles.numberRow}><View style={styles.quantityField}><Text style={styles.inputLabel}>수량</Text><TextInput accessibilityLabel={`작업 항목 ${itemNumber} 수량`} keyboardType="number-pad" value={draft.quantityText} onChangeText={(value) => update({ quantityText: digitsOnly(value) })} placeholder="1" placeholderTextColor={colors.faint} style={styles.input} /></View><View style={styles.amountField}><Text style={styles.inputLabel}>항목 금액</Text><TextInput accessibilityLabel={`작업 항목 ${itemNumber} 금액`} keyboardType="number-pad" value={draft.lineAmountText} onChangeText={(value) => update({ lineAmountText: digitsOnly(value) })} placeholder="예: 85000" placeholderTextColor={colors.faint} style={styles.input} /></View></View>
  </View>;
}

function digitsOnly(value: string) { return value.replace(/[^0-9]/g, ''); }

const styles = StyleSheet.create({
  screen:{backgroundColor:colors.canvas,flex:1},content:{paddingBottom:38,paddingHorizontal:20,paddingTop:25},topBar:{alignItems:'flex-start',flexDirection:'row',justifyContent:'space-between',marginBottom:14},kicker:{color:colors.orange,fontSize:14,fontWeight:'800',marginBottom:5},title:{color:colors.ink,fontSize:31,fontWeight:'800',letterSpacing:-.5},subtitle:{color:colors.muted,fontSize:14,marginTop:6},roleButton:{backgroundColor:colors.surface,borderColor:colors.border,borderRadius:12,borderWidth:1,marginTop:8,paddingHorizontal:11,paddingVertical:9},roleButtonText:{color:colors.tealDark,fontSize:12,fontWeight:'800'},identityCard:{alignItems:'center',backgroundColor:colors.ink,borderColor:colors.ink,flexDirection:'row',marginBottom:28,marginTop:18,padding:17},identityIcon:{alignItems:'center',backgroundColor:'#3A555A',borderRadius:17,height:50,justifyContent:'center',marginRight:12,width:50},identityGlyph:{color:'#D7ECE5',fontSize:23,fontWeight:'800'},identityCopy:{flex:1},identityTitle:{color:'#FFF',fontSize:16,fontWeight:'800'},identityText:{color:'#B7C8C8',fontSize:12,lineHeight:18,marginTop:4},jobList:{gap:11,marginBottom:20},jobCard:{gap:11,padding:17},jobTop:{alignItems:'center',flexDirection:'row',justifyContent:'space-between'},jobDue:{color:colors.muted,fontSize:12,fontWeight:'700',maxWidth:'55%',textAlign:'right'},customer:{color:colors.ink,fontSize:19,fontWeight:'800'},device:{color:colors.muted,fontSize:13},issueRow:{alignItems:'center',flexDirection:'row'},issueDot:{backgroundColor:colors.orange,borderRadius:4,height:8,marginRight:7,width:8},issue:{color:colors.ink,fontSize:14,fontWeight:'700'},emptyTitle:{color:colors.ink,fontSize:18,fontWeight:'800'},emptyText:{color:colors.muted,fontSize:14,lineHeight:21,marginTop:7},backButton:{alignSelf:'flex-start',minHeight:44,justifyContent:'center'},backText:{color:colors.teal,fontSize:16,fontWeight:'800'},detailHeader:{marginBottom:18,marginTop:8},detailTitle:{color:colors.ink,fontSize:28,fontWeight:'800',letterSpacing:-.4,lineHeight:35,marginTop:12},detailMeta:{color:colors.muted,fontSize:14,marginTop:7},verifyCard:{backgroundColor:colors.ink,borderColor:colors.ink,marginBottom:19},cardKicker:{color:colors.orange,fontSize:12,fontWeight:'800',letterSpacing:.5},verifyCode:{color:'#FFF',fontSize:30,fontWeight:'900',letterSpacing:1,marginTop:10},verifyModel:{color:'#D7E4E2',fontSize:15,fontWeight:'700',marginTop:3},verifyHelp:{color:'#B7C8C8',fontSize:13,lineHeight:19,marginTop:10},darkInput:{backgroundColor:'#2D494E',borderColor:'#526A6E',borderRadius:13,borderWidth:1,color:'#FFF',fontSize:17,fontWeight:'800',marginTop:12,minHeight:52,paddingHorizontal:14},scanButton:{alignItems:'center',borderColor:'#6A8588',borderRadius:12,borderWidth:1,justifyContent:'center',marginTop:9,minHeight:46},scanButtonText:{color:'#D7ECE5',fontSize:14,fontWeight:'800'},verifyStatus:{borderRadius:11,marginTop:9,padding:10},verifyStatusIdle:{backgroundColor:'#2C4448'},verifyStatusOk:{backgroundColor:'#164F46'},verifyStatusText:{color:'#D0D9D8',fontSize:12,fontWeight:'700'},verifyStatusTextOk:{color:'#BCEBDC'},safeCopy:{color:colors.muted,fontSize:14,lineHeight:21,marginTop:10},stageRow:{flexDirection:'row',justifyContent:'space-between',marginBottom:19,paddingHorizontal:6},stageItem:{alignItems:'center',flex:1},stageDot:{alignItems:'center',backgroundColor:'#E7EBE8',borderRadius:18,height:36,justifyContent:'center',width:36},stageDotActive:{backgroundColor:colors.teal},stageNumber:{color:colors.muted,fontSize:14,fontWeight:'800'},stageNumberActive:{color:'#FFF'},stageLabel:{color:colors.muted,fontSize:12,fontWeight:'700',marginTop:6},actionCard:{marginBottom:14},actionTitle:{color:colors.ink,fontSize:20,fontWeight:'800',lineHeight:27,marginTop:7},actionText:{color:colors.muted,fontSize:14,lineHeight:21,marginTop:6},factRow:{borderTopColor:colors.border,borderTopWidth:1,flexDirection:'row',justifyContent:'space-between',marginTop:15,paddingTop:13},factLabel:{color:colors.muted,fontSize:13,fontWeight:'700'},factValue:{color:colors.ink,fontSize:13,fontWeight:'800',maxWidth:'65%',textAlign:'right'},errorBox:{backgroundColor:colors.orangeWash,borderRadius:15,marginBottom:14,padding:14},errorText:{color:'#8D4A27',fontSize:14,lineHeight:20},refreshText:{color:colors.tealDark,fontSize:14,fontWeight:'800',marginTop:9},formCard:{marginTop:2},formTitle:{color:colors.ink,fontSize:21,fontWeight:'800'},scheduleError:{color:'#9A4127',fontSize:13,fontWeight:'700',marginBottom:12},formError:{color:'#9A4127',fontSize:13,fontWeight:'700',lineHeight:19,marginBottom:12},itemList:{gap:13,marginTop:16},itemCard:{backgroundColor:colors.canvas,borderColor:colors.border,borderRadius:15,borderWidth:1,padding:14},itemHeader:{alignItems:'center',flexDirection:'row',justifyContent:'space-between'},itemTitle:{color:colors.ink,fontSize:17,fontWeight:'800'},removeButton:{justifyContent:'center',minHeight:44,paddingHorizontal:8},removeText:{color:'#9A4127',fontSize:13,fontWeight:'800'},inputLabel:{color:colors.ink,fontSize:15,fontWeight:'800',marginTop:18},chips:{flexDirection:'row',flexWrap:'wrap',gap:8,marginTop:9},chip:{backgroundColor:colors.surface,borderColor:colors.border,borderRadius:20,borderWidth:1,minHeight:44,justifyContent:'center',paddingHorizontal:12},chipActive:{backgroundColor:colors.tealWash,borderColor:colors.teal},chipText:{color:colors.muted,fontSize:13,fontWeight:'700'},chipTextActive:{color:colors.tealDark},numberRow:{flexDirection:'row',gap:10},quantityField:{flex:0.35},amountField:{flex:0.65},input:{backgroundColor:colors.surface,borderColor:colors.border,borderRadius:13,borderWidth:1,color:colors.ink,fontSize:17,marginTop:8,minHeight:54,paddingHorizontal:12},addItemButton:{alignItems:'center',borderColor:colors.teal,borderRadius:13,borderStyle:'dashed',borderWidth:1,justifyContent:'center',marginTop:13,minHeight:50},disabledButton:{opacity:.45},addItemText:{color:colors.tealDark,fontSize:14,fontWeight:'800'},totalBox:{alignItems:'center',backgroundColor:colors.ink,borderRadius:14,flexDirection:'row',justifyContent:'space-between',marginVertical:16,padding:15},totalLabel:{color:'#D7E4E2',fontSize:14,fontWeight:'700'},totalValue:{color:'#FFF',fontSize:21,fontWeight:'900'},reviewList:{gap:9,marginTop:16},reviewItem:{backgroundColor:colors.canvas,borderRadius:12,padding:13},reviewItemTitle:{color:colors.ink,fontSize:15,fontWeight:'800'},reviewItemMeta:{color:colors.muted,fontSize:13,marginTop:5},editButton:{alignItems:'center',borderColor:colors.border,borderRadius:13,borderWidth:1,justifyContent:'center',marginBottom:10,minHeight:48},editButtonText:{color:colors.tealDark,fontSize:14,fontWeight:'800'},waitCard:{backgroundColor:colors.tealWash,borderColor:'#CDE4DD'},waitTitle:{color:colors.tealDark,fontSize:19,fontWeight:'800'},loader:{marginTop:16},
});
