# ML service

Python/PyTorch 기반 학습·평가 작업을 보관합니다.

두 모델 계열을 분리합니다.

1. 모바일 센서·GPS 데이터 품질 및 이동 유형 보조 판별
2. 수리·점검·누적 사용량 기반 time-to-inspection 위험 추정

LLM 보고서 생성은 이 디렉터리의 모델 책임이 아닙니다. 모든 모델은 dataset version, feature version, split strategy, metric, calibration, abstention을 함께 기록해야 합니다.
