{
  "name": "@mobility-reliability/firebase-rules",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "pnpm test",
    "test": "firebase emulators:exec --project demo-mobility-reliability --only firestore,storage \"vitest run\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.16.0",
    "firebase-tools": "15.24.0",
    "vitest": "4.1.10"
  }
}
